__pycache__/
*.pyc
*.egg-info/
dist/
build/
*.wav
*.mp3

# User data (settings, history, logs) — travels with the checkout but
# never committed.
voxtype/data/

# Runtime dirs (created by setup.ps1)
mcp-venv/
stt-venv/
tts-venv/
Kokoro-FastAPI/
.claude/
_*.py
