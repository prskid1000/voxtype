__pycache__/
*.pyc
*.egg-info/
dist/
build/
*.wav
*.mp3
# …except the bundled audio cues (1 s start/stop/done chimes).
!voxtype/resources/sounds/*.wav

# User data (settings, history, logs) — travels with the checkout but
# never committed.
voxtype/data/

# Runtime dirs (created by setup.ps1)
mcp-venv/
stt-venv/
tts-venv/
voxtype-venv/
Kokoro-FastAPI/
.claude/
_*.py
!tests/__init__.py
!tests/_isolate.py

.docgraph/
